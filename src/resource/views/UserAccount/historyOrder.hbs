<section class="shopping-cart-container">
            {{#if order}}
            {{#each order}}
    <div class="products-container">
                {{!-- <form action="#" method="get">
                <input type="hidden" name="orderId" value={{this.orderId}}>
                <input type="submit" value="" id = "submitbutton">
                <label for="submitbutton" class = "link-button"> Detail</label>
            </form> --}}
            <a href="/user/order-detail?orderId={{this.orderId}}" class="header_order">
                <h1 class="order_date">{{standardDateOrder this.date}}</h1>
            {{#if this.status}}
                <p class="order_status">Have been delivered</p>
            {{else}}
                <p class="order_status">Waiting for delivery</p>
            {{/if}}
            </a>
            {{#each this.items}}
        <div class="box-container">
            <div class="box order">
                <img src={{this.image}} alt="">
                <div class="content" style="width:60%;">
                    <h3>{{this.productName}}</h3>
                    <span> quantity : {{this.quantity}} </span>
                    <br>
                    <span> price : </span>
                    <span class="price"> {{this.price}} </span>
                </div>
            </div>
                        </div>

                        {{/each}}

            </div>
            <br>
            <br>
            {{/each}}
            {{else}}
            <h3 class="title">your orders</h3>
            <h1>Empty</h1>
           {{/if}}
        </div>
                    </section>


